@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>
    @using (Html.BeginForm("Submit", "Home", FormMethod.Post, new { @name = "Form1" }))
    {
        @Html.DropDownList("FruitId", new List<SelectListItem> { new SelectListItem { Text = "English", Value = "en" }, new SelectListItem { Text = "Arabic", Value = "ar" } }, "Please select", new { @id = "ddlLaguage" })
    }

    <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
    <script>
        window.jQuery || document.write('<script src="http://mysite.com/jquery.min.js"><\/script>'))
    </script>
    <script type="text/javascript">
           $(document).ready(function () {
       
               $("body").on("change", "#ddlLaguage", function () {
                   debugger;
              var LanguageValue = ddlLaguage.value;
                $.ajax({
                    type: "POST",
                    contentsType: "application/json;charset=utf-8",
                    url: "/Language/Change",
                    data: { LanguageAbbrevation:  ddlLaguage.value},
                    success: function (Data) {

                    },
                    Error: function (XMLHttpRequest, textStatus, errorThrown) {
                    }
                });
            });
            //url.action('Language', 'Change', new { LanguageAbbrevation: LanguageValue })
        });
    </script>
</body>
</html>